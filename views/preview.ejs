<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0" />
  <title>Image preview</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      background: #000;
      color: #eee;
      font-family: ui-monospace, 'IBM Plex Mono', SFMono-Regular, Menlo, monospace;
      overflow: hidden;
    }

    .stage {
      position: fixed;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: zoom-in;
    }

    /* The pixelated teaser. An extra CSS blur makes it read as "locked / low-res". */
    #preview {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
      filter: blur(6px);
      transform: scale(1.03);          /* hide blurred edges */
      transition: filter 0.5s ease, transform 0.5s ease, opacity 0.4s ease;
      user-select: none;
      -webkit-user-drag: none;
    }

    /* Dark vignette so the call-to-action pops. */
    .scrim {
      position: fixed;
      inset: 0;
      background: radial-gradient(ellipse at center, rgba(0,0,0,0.25) 0%, rgba(0,0,0,0.75) 100%);
      pointer-events: none;
      transition: opacity 0.4s ease;
    }

    .cta {
      position: fixed;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.9rem;
      text-align: center;
      pointer-events: none;
      transition: opacity 0.35s ease;
      padding: 1rem;
    }

    .badge {
      width: 74px;
      height: 74px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0, 255, 0, 0.08);
      border: 2px solid #0f0;
      box-shadow: 0 0 0 0 rgba(0, 255, 0, 0.5);
      animation: pulse 2s infinite;
    }
    .badge svg { width: 34px; height: 34px; color: #0f0; }

    @keyframes pulse {
      0%   { box-shadow: 0 0 0 0 rgba(0, 255, 0, 0.45); }
      70%  { box-shadow: 0 0 0 22px rgba(0, 255, 0, 0); }
      100% { box-shadow: 0 0 0 0 rgba(0, 255, 0, 0); }
    }

    .cta-title {
      font-size: clamp(1.05rem, 4vw, 1.5rem);
      font-weight: 700;
      color: #0f0;
      letter-spacing: -0.02em;
    }
    .cta-sub {
      font-size: clamp(0.8rem, 2.6vw, 0.95rem);
      color: #7a7a7a;
    }

    .stage.revealed { cursor: default; }
    .stage.revealed #preview { filter: none; transform: scale(1); }
    .stage.revealed .scrim,
    .stage.revealed .cta { opacity: 0; }

    .spinner {
      display: none;
      width: 34px; height: 34px;
      border: 3px solid rgba(0,255,0,0.2);
      border-top-color: #0f0;
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
    }
    @keyframes spin { to { transform: rotate(360deg); } }
    .loading .badge, .loading .cta-title, .loading .cta-sub { display: none; }
    .loading .spinner { display: block; }
  </style>
</head>
<body>
  <div class="stage" id="stage" title="Click to view full resolution">
    <img id="preview" src="/<%= code %>?img=blur" alt="preview" />
    <div class="scrim"></div>
    <div class="cta" id="cta">
      <div class="badge">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="7"></circle>
          <line x1="16.5" y1="16.5" x2="21" y2="21"></line>
          <line x1="11" y1="8" x2="11" y2="14"></line>
          <line x1="8" y1="11" x2="14" y2="11"></line>
        </svg>
      </div>
      <div class="spinner"></div>
      <div class="cta-title">View full resolution</div>
      <div class="cta-sub">tap the image to load HD</div>
    </div>
  </div>

  <script>
    (function () {
      const stage = document.getElementById('stage');
      const img   = document.getElementById('preview');
      const cta   = document.getElementById('cta');
      let done = false;

      function reveal() {
        if (done) return;
        done = true;
        cta.classList.add('loading');           // show spinner while HD loads
        const full = new Image();
        full.onload = function () {
          img.src = full.src;                   // swap in the crisp original
          stage.classList.add('revealed');
        };
        full.onerror = function () {            // fall back: just unblur what we have
          stage.classList.add('revealed');
        };
        full.src = '/<%= code %>?img=full';
      }

      stage.addEventListener('click', reveal);
    })();
  </script>
</body>
</html>
